@model IEnumerable<ASS.Model.ass_reminder>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descs)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.clases)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.start_day)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.end_day)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.isOnlyMe)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descs)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.clases)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.start_day)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.end_day)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.getIsOnlyMe)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
